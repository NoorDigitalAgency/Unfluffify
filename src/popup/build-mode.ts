/** Replaced with a literal by WXT. Keeping the check in one tiny module gives
 * Rollup a single branch to erase from production bundles. */
export function popupDebugBuildEnabled(): boolean {
  return __UF_DEBUG_BUILD__;
}
