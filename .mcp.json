{
  "mcpServers": {
    "playwright-local": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--user-data-dir=__UNFLUFFIFY_REPO_ROOT__/.wxt/browser-profile",
        "--config=__UNFLUFFIFY_REPO_ROOT__/.vscode/browser-mcp.config.json"
      ]
    }
  }
}