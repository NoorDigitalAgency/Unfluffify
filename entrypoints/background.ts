import { defineBackground } from "wxt/utils/define-background";

// A1 bootstrap placeholder so `wxt prepare` can initialize the WXT workspace
// before the real runtime adapters are introduced in Phase A2.
export default defineBackground(() => {});
