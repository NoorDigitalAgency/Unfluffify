{
  "name": "unfluffify",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "package:extension": "deno task build:release && deno task package",
    "test": "deno task test"
  }
}
