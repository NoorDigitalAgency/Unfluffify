# Study — Agent Structured Returns (2026-08-14 run)

Each successful study agent's own summary, key findings, and the product-owner questions it
raised. The full reports are the sibling `.md` files; **this file is the only record of the
key findings and open questions**, which the agents returned rather than wrote to disk.

Four agents (`rewrite-implementation-state`, both syntheses, and the completeness critic)
died on the org's monthly spend limit and returned nothing — see `RESUME.md` §5.

---

## legacy-locked-contracts

### Summary

Digested all 13 requested legacy docs (MARKING_AND_HIGHLIGHTING_LOGIC.md, PROPERTY_LOCK.md, knowledge.md, both content-marking and marking-algorithm doc sets incl. 04-qa-decisions, marking-implementation-plan, reflex-arc-plan, muscle-memory-inventory (superseded pointer), widening-hardening-plan, marking-widening-review, HANDOFF.md, plan.md, lifecycle-resume-plan.md) into a register at the report path. Part 1 catalogs ~80 locked contracts tagged [ALG]/[UX]/[PROTO]: the "seed then step aside" marking invariant (selectors/defaults are initial-state only; stored rows are sole truth), exact immutable/toggleable taxonomies, session-scoped fresh-on-enable marking data, explicit-flag/normalization/closed-include rules, the widening ladder with all five locked restraints (F1-F5, landmark-only shell rejection), the deriveMarkMode FSM, shadow-DOM Googlebot-parity flattening, CSS-clamp visibility, the full AI-submission row rules (page-height/mobile-width geometry), CP7a/CP7b performance contracts, the one-ritual-per-visit reveal/freeze with freeze-at-absolute-bottom and navigation-only release, consent-on-every-property-page with the high-precision selector allowlist, the property-lock protocol with exact timing constants (30s heartbeat/30min idle/70s countdowns/30s cross-property/60s passive), the popup button-state matrix with the deterministic non-fingerprint dirty axis, the single-curtain + spinner-surface-vocabulary doctrine with MV3 fail-open recovery, mobile-emulation forcing, save/coverage backend rules (empty responses never destroy local; save-response replace), Send-to-Lynx fail-closed guard, and the brain/signal epoch-and-provenance doctrine. Part 2 is a 35-item fixed-bug pain register plus 16 deferred items, 7 retracted misdiagnoses, and the live-QA gotcha canon. Part 3 records every locked QA decision (D1-D6, C1-C3, MA-1..MA-4, F1-F8, D-SAVE/D-BUS/D-SCOPE/D-ROLLOUT, verbatim user dirty-contract). Part 4 is a 12-point must-not-lose checklist for the rewrite.

### Key findings

- THE load-bearing invariant (architect-authoritative, MHL:23-43): defaults and CSS/AI selectors only SEED initial rows then STEP ASIDE; stored rows are the sole marking truth; an un-excluded {excluded:false} element must render/submit as implicit content even if a rule still matches it. Three separate live bugs (blank-on-unmark, seed evaporation, unmark reversion) came from violating it.
- Exact locked taxonomies: immutable = IMG,INPUT,NOSCRIPT,SELECT,TITLE,STYLE,SCRIPT,TEMPLATE,IFRAME,VIDEO,SVG (payload tag-list, never per-page rows); toggleable = FOOTER,FORM,LABEL,NAV,HEADER,DIALOG,ASIDE,BUTTON; LINK deliberately omitted; tag matching case-insensitive both sides; toggleable auto-exclusion is unconditional on the tag (F1 deviation from 052c).
- Marking data is session-scoped: every enable recomputes fresh from defaults+selectors, discards stale drafts, backend-saved markings never pre-populate, and marking disables on ANY navigation/reload; dirty is deterministic (real toggle click only, never fingerprints) per verbatim user contract.
- AI submission is a locked enumerated payload (QA D3): visible textual content as included rows + shallow-boundary exclusions; every stored excluded row submits excluded unless explicitly included or ancestor-suppressed; visibility at save uses page-HEIGHT/mobile-WIDTH geometry; CSS-clamped text is included (MA-1b); /html[1] and /html[1]/body[1] never submitted; positional XPaths aligned to the sanitized snapshot.
- Reveal/freeze contract: exactly ONE ritual per page visit (load-complete or render-mode-set exit only, never in marking mode or re-inspect); lazy-load lock at 50% of initial height, max ONE expansion, full freeze engages only at the absolute bottom, return scroll under freeze; the page-visit freeze is a single lock released only on navigation — per-subsystem resumes must never unfreeze.
- Consent hiding runs on ALL configured property pages, decoupled from candidacy/reveal/marking; REMOVABLE_ELEMENT_SELECTORS is a high-precision allowlist (never add banner/notice/toast/paywall/cmp) validated against live included-content counts.
- Property lock: sessionStorage client ID (not tab ID) with cloned-tab rotation; 30s heartbeats while interacted <30min; 70s connectivity and off-candidate countdowns; 30s cross-property recovery cooldown; 60s passive release countdown; tab close releases immediately; editor stops calling /load after one bootstrap fetch; passive observers refresh at most once/minute.
- Popup preview-exit matrix is locked: page locked ONLY during compute/preview; post-AI edit re-enters MARKING_DIRTY with Run AI re-enabled; discard settles marking-active publishing a full clean-reset fact patch whose previewActive/previewBlocked/aiRunUpToDate fields are load-bearing for brain authority handover; saved lands in SILENT (D-SAVE).
- Spinner doctrine: brain broadcasts surface vocabulary only (never display strings); no popup-local spinner state; single curtain (suppressNotice orchestration); MV3-suspension recovery via deadline+30s fail-open projection and swKeepAlive — a stuck spinner class came from selections persisting while the queue lived in SW memory.
- The #5/#14 post-exit collapse class (stale interleaved refreshUi passes publishing isEnabled:false) was fixed at source by removing the refresh cadence (P5) plus epoch/latch discipline; doctrine: gate at the moment of effect with pass-epochs, time windows can never fix this class; signals must be born at the source with provenance (the false markings-changed signal came from an internal config-merge reshape).
- Pain register documents the legacy analogue of the production /save wipe: the /save POST uploaded pageMarkings:{} because the marked page never reached shared config at upload time (blank pageType -> key '' -> dropped from payload); fixed via pageType repair + resolved-pageType filter + save-response replace; empty/partial /load//save responses must never replace local snapshots.
- Deferred (never fixed in legacy): same-URL-refresh popup session reset, narrow brain tab-reset on navigation, fail-open API audit (task #18), stacked spinner cards (#16), light-only collapsed-textual-fallback rects in shadow, motion-lock reassert cost cap on Webflow pages, dead DISCARDING phase, decision-dead aiRunMarkingsFingerprint field.
- Shadow DOM is a locked contract (MA-1/D5a, shipped CP4-CP6): flatten open shadow roots to real DOM with Googlebot parity across capture, enumeration, XPath (continuous positional), target resolution, and render; closed roots skipped; all descent gated so shadow-free pages are byte-identical.
- Widening restraints locked: ancestor ladder is self-markable-only with a hard stop at body (root exclusion impossible); descendants-only targets rejected landmark-based-only at any depth (broad-footprint check deliberately dropped because real footers are dimensionally indistinguishable from content columns); >=2 markable descendants required; textless children filtered from group cohesion.

### Open questions raised

- Does the rewrite adopt the legacy user-specified page-block scope verbatim (block the page only when popup is busy AND interaction can affect results: reveal/freeze, AI run, save) or the stricter earlier rule (every popup curtain blocks the page)? Legacy docs record both, with the relaxed version as the last user word (lifecycle-resume-plan.md:388-390).
- The curtain/spinner ownership migration to per-layer orchestrators (architect direction, knowledge.md:549) was never completed in legacy — the brain is still authoritative for sessionCurtainPhase. Should the rewrite treat the layer-orchestrator model as the binding target architecture, or is the shipped brain-authoritative P4-P6 state the contract?
- Task #18 (audit all fail-open API calls and decide which flip to fail-closed) was explicitly queued for the architect's decision table and never resolved — the rewrite needs that decision table to know which network failures must block vs degrade.
- Legacy forces desktop-preview availability only when the property already has AI selectors and disables marking entry while it is on; is that gating a product requirement to preserve, or an implementation artifact of the CDP emulation debugger that the rewrite may relax?
- The accepted F2 tradeoff (landmark-less full-width columns are widen-eligible again) was called an 'accepted tradeoff' by the architect for the bonliva footer case — does the product owner accept the same over-widening risk in the rewrite, or want a different restraint now that the code is being rebuilt?
- The pageTypeAssignments feature flag stayed false pending a backend endpoint (HANDOFF.md:185) — is that endpoint now live, and is the feature in scope for the rewrite?
- Legacy's off-candidate same-property pages keep silent highlighting and lock visibility while blocking only marking entry; confirm the rewrite must reproduce this exact partial-degradation UX rather than collapsing the page UI.

---

## legacy-content-ux

### Summary

Produced an exhaustive in-page UX/visual spec of legacy Unfluffify from full reads of core.ts (14,312 lines), content-main.ts (7,557 lines), the freeze bridge/control, spinner contract, emulation, cursors, and the locked marking contract. The report documents: the complete injected-chrome inventory; the marking overlay (#unfluffify-overlay, z-index 2147483647, 10 layers z2-z11 with exact per-layer semantics); every overlay class with exact CSS (uf-hover amber 2px, uf-focus cyan 3px blinking, uf-hard-locked red/orange candy-stripe, uf-default 1px green, uf-ai-content animated marching green dashes + overlay/ghost variants, uf-explicit-include/exclude 3px solid + dotted/dashed ghosts, uf-interaction-ack 160ms pulse); rect mechanics (per-client-rect boxes, paint-reachability 5-point hit tests, ghost/collapsed-text fallbacks, scroll hiding); the marking FSM and click semantics (first-click unmark of default boundaries, Alt-include, Shift widening ladder with page-shell guards, Space passthrough with Alt+link navigation, 320ms duplicate suppression, busy-locked dimming + notices); custom SVG cursors with hotspot 4 3 and deliberate neutral fallbacks; the one-per-visit reveal/freeze ritual (consent hide, curtain + input blockers, 10x smooth scroll walk with 1s dwells, lazy-load suppression at 50%, freeze at absolute bottom, page-visit lock released only on navigation) and the snowflake+code-tags pause indicator; consent chrome hiding (selector list, opacity/visibility/pointer-events + dialog.close(), scroll repair, bypass style); silent mode (z-index 2147483646, immutable/content/excluded layers with exact colors, tooltip annotations + click-to-copy, anti-blink settle choreography); AI preview page affordances (clickable elements, yellow flash focus, compute-lock curtain); render-mode inspection flow with 30s watchdog; legacy viewport-only mobile emulation (412x960, no UA spoofing — the rewrite added that); page-visit lifecycle; property-lock banner modes; icon-only action feedback; full toast/notice copy catalog and a timing-constants table. All claims carry file:line citations.

### Key findings

- Marking overlay stacking is a locked visual hierarchy: #unfluffify-overlay at z-index 2147483647 with 10 layers (hard z2 ... interaction z11); 'saved-explicit-*' layers are the fresh session baseline recomputed at enable, NOT backend-fetched marks (core.ts:7307-7334, 10086-10129)
- Exact mark colors: hover #ffb300/2px, focus #00acc1/3px blinking 1Hz with glow, immutable = 45deg red/orange repeating gradient stripes + 2px dashed rgba(225,70,70,.4), default = 1px #2e7d32 on rgba(46,125,50,.08), explicit include #1b5e20/3px on 20% fill, explicit exclude #c62828/3px on 20% fill, AI content = animated 6px marching dashes #35943a cycling 24px per 2s (core.ts:7349-7426)
- Toggleable-default exclusions and selector-excluded matches have NO dedicated overlay: defaults render through the ordinary red exclude overlay, selector excludes only suppress the default layer (locked contract, MARKING_AND_HIGHLIGHTING_LOGIC.md:53-54,751-753)
- First exclude-click on an auto-excluded default boundary writes {excluded:false} and unmarks it on ONE click; a 160ms uf-interaction-ack pulse acknowledges every toggle before recompute; duplicate clicks suppressed within 320ms (core.ts:9464-9483, 8332-8361; marking-rules.ts:132-170)
- Reveal/freeze ritual is one-per-page-visit with join semantics: consent hide, progress cursor + dim curtain + centered spinner card + capture-phase input blocker over 22 event types, up to 10 smooth scrolls with 1s dwells, lazy-load suppression engaged at 50% height in the MAIN world (document_start-armed wrappers), freeze at the ABSOLUTE bottom, release only on navigation via the page-visit reason (core.ts:5245-5336, 7680-7747, 861-868)
- While frozen, a 48x30px top-right pill shows snowflake+code-tags glyphs from a content-injected MDI font ('Page motion paused'); freeze = CSS animation/transition kill + Web Animations/SVG/media pause + inline locks on <=800 motion candidates + synthetic hover-pause + page-world timer deferral (core.ts:5654-5767, 6976-7021)
- Silent mode draws into a separate overlay one z-index below marking (2147483646) that never captures clicks: uf-silent-content 2px dashed #44b532/8% fill, uf-silent-excluded 2px dashed #b03b3b/8% fill, uf-silent-immutable 1px dashed rgba(156,107,107,.45); every highlighted node gets a rewritten title tooltip (selector+XPath) and left-click copies it to the clipboard (content-main.ts:2086-2131, 3022-3205)
- Consent chrome hiding runs on ALL configured property pages, is visual-only (opacity/visibility/pointer-events + dialog.close(), DOM intact), repairs scroll locks, and injects an aria-hidden pointer-events bypass (content/constants.ts:10-39, core.ts:2555-2604, 11828-11899)
- Single-spinner contract: a page-blocking brain curtain raises the popup-busy overlay (rgba(16,20,28,.2) dim + spinner card, fail-open watchdog 65s default/10min max) and suppresses the inspection notice spinner so two spinners never stack; curtain copy resolves machine overlay-memory first (compute_lock='Analyzing page content with AI...' with hard input block), then the shared phase table (content-bus-client.ts:105-147, core.ts:5442-5652, overlay-memory.ts:38-75)
- Custom cursors: 32px SVG arrow + badge circle (red minus = exclude, green plus = include), hotspot 4 3, fallbacks deliberately crosshair/copy (never not-allowed, which flashed the forbidden cursor), preloaded via new Image() (core.ts:7280-7306, 7236-7258)
- Legacy mobile emulation is viewport-only: debugger Emulation.setDeviceMetricsOverride 412x960 mobile with auto-fit scale (0.25-1); mobile forced on for the active marking editor tab; NO userAgent/client-hints spoofing (that is a re-write addition, commit 489649d8) (common/emulation.ts:367-462, constants.ts:19-32)
- Action feedback is icon-set-only (icons/active vs icons/default per tab enabled state); there is no badge text or badge color anywhere (utilities.ts:754-798)
- Navigation (full or SPA via patched history) is the ONLY freeze release point and always disables marking, discarding unsaved drafts per the fresh-session contract (core.ts:11731-11768)
- uf-explicit-exclude-ghost is styled in CSS but never emitted by drawCollections (hidden excludes route to the hard layer) - a re-implementation should not treat it as a live visual state (core.ts:7423-7426 vs 11367-11370)

### Open questions raised

- Should the rewrite keep the marking overlay click-capture model (right-click/contextmenu also commits a mark, core.ts:9715-9717), or reserve right-click for the browser context menu?
- The legacy freeze is page-visit-sticky: after disabling marking the page stays frozen (animations/media halted) until navigation. Is that the desired product behavior for the rewrite, or should disabling fully release the page?
- Legacy hides consent chrome on every configured property page even for passive visitors who are not editing - is silently suppressing cookie/consent banners for all team members on production customer sites the intended scope, given GDPR-consent implications of the operator never being able to interact with the banner?
- Does the rewrite need to preserve the click-to-copy tooltip affordance in silent mode (left-click on highlighted content copies selector+XPath), which hijacks normal page clicks on annotated nodes?
- Should the rewrite adopt the legacy one-per-visit reveal ritual timings (up to 10 scroll passes x 1s dwell, potentially ~20-30s of blocked input on long pages) or is a faster/interruptible reveal acceptable?
- Is the debugger-based viewport emulation (with Chrome's 'started debugging' infobar) acceptable long-term, and should the rewrite's UA/client-hints spoofing (Pixel 7/Android 13) be backported as the canonical behavior for parity of what editors mark vs what the AI judges?

---

## rewrite-architecture

### Summary

The rewrite (branch re-write, HEAD 5c21aaaf, ~13.1k non-test lines, 485/485 tests green) is a genuine clean-room reimplementation whose lower layers honor the .reimplementation contract well: the pure Zod domain spine (single-pass evaluate producing overlay+rows, branch-scoped recompute, unified rows[], fixed clamp-visibility), the 12-state popup FSM with a frozen per-state memory matrix (incl. the editor_preparing exemption), one typed bus with idempotent-by-sequence replies, a real nonce-armed single page-world program, token rotation on every authed call, backend-issued lock identity, and a well-reasoned backend-authoritative local-data rule. But the authority model is inverted at the top: the 1,800-line popup entrypoint (src/entrypoints/popup/main.tsx, ~45 module-level mutable variables grown over the last ~15 commits) is the de-facto orchestrator. It births 12 of 16 signal names via signals.emit (bypassing fold→decide, so the brain's run/preview/save/reconciliation deciders are dead code in the live path), composes and dictates the content organ's curtain/banner surface on a 500ms poll, keeps a shadow state bag (preLockPopupState + hand-built FSM states), re-derives session truth from content status, and even fabricates source:"brain" signals offline — the legacy dictation-model/dual-bag disease re-growing in a new host, though confined to one file with clean seams underneath. Signal delivery is pull-only (push channel never emitted; server-side consumed-once cursor implemented but unused; content consumes no signals at all — it is driven by popup-composed directives). Persistence is write-only: durable facts are persisted on every observe but never rehydrated, so an MV3 SW restart resets the per-tab seq to 0 while the popup's cursor survives, silently starving the popup of signals. Contract-critical gaps: /save posts a single-page snapshot and discards the response (destructive-save hazard if the owned backend does full-replace, INV-6.5); static render mode cannot produce a valid submission (schema demands rawHtml iff static, but no static-HTML fetcher exists — Run AI always fails on static properties); discard resets to defaults-only, not defaults+AI-selectors (INV-6.6); the INV-6.4 two-gate AI machinery is dead code; the lock lifecycle is popup-tethered (heartbeats only while the popup polls; no reconnect/probes/off-candidate/cooldown/tab-close release); preview is a stub. Verdict: vertical entanglement (god-file fusion) is solved; horizontal entanglement (who decides/owns/moves) migrated into the popup instead of the brain — better than legacy, but not the reflex arc, and trending the wrong way.

### Key findings

- The popup entrypoint src/entrypoints/popup/main.tsx (1,800 lines, ~45 module-level lets) is the system's real orchestrator: it emits 12 of 16 signal names itself via signals.emit with source:'popup' (run.*, session.saved/discarded, preview.opened, reconciliation.*, marking.enabled/disabled), bypassing the brain's fold→decide loop entirely (rewrite-brain.ts:24-37 emitSourceSignal appends without folding)
- The brain's decide.ts covers all 16 signals and is unit-tested, but in the live path only 4 are actually fact-decided (markings.changed from a monotonic toggle counter, marking.enabled/disabled, session.navigated); runPhase/previewActive/savedSeq facts are never populated — the tested decider machinery is dead code awaiting wiring
- The popup composes the content organ's full curtain/banner/blocked surface (composeContentDirective, main.tsx:675-708) and pushes it to content on every 500ms poll tick — the retired 'dictation model' relocated from background to popup; content consumes zero brain signals and is driven purely by commands/directives
- MV3 persistence is write-only: persistDurableFacts runs on every observe (background/index.ts:129-173) but rehydrateDurableFacts has zero live callers; after SW restart the per-tab signal seq resets to 0 while the popup's cursor survives, so post-restart signals with seq <= cursor are silently dropped (signal-cursor claim + machine.ts:73 dedupe)
- Save posts a SINGLE-page ConfigSnapshot (configFromSubmission, main.tsx:854-875) with submittedSelectorsFingerprint:'' and the /save response is discarded (index.ts:300-309) — violating INV-6.5 ('all locally-marked pages as one snapshot; response replaces local'); if the owned backend does full-replace, saving page B wipes pages A/C — the same destructive-save class seen live on legacy production
- Static render mode is structurally broken: AiRunPayloadSnapshotSchema requires rawHtml iff static (domain/schema/submission.ts:34-49) but no static-HTML fetcher exists anywhere (only fetch is the JSON transport), so Run AI/Save on a static property always fails at capture
- Lock lifecycle is popup-tethered: heartbeats fire only inside lock.directive handling (lock-runtime.ts:232) which only the popup's 500ms poll calls — closing the popup stops heartbeats within 30s; no reconnect/backoff, no HTTP reachability probes (isNetworkReachable dead), no off-candidate/cross-property/dispose-grace timers, no tab-close release, no takeover UI (client methods exist unwired)
- Consumed-once infrastructure is implemented but unused: pullForOrgan/markConsumed/signals.consume exist server-side (brain/signals.ts:54-60, index.ts:134-157) but no caller passes organId or consumes; push channel signal.emitted is never emitted by the background — delivery is 500ms popup pull only; signal log is a 128-entry ring so a burst larger than that is unrecoverable
- Dual-state-bag pattern returned: preLockPopupState + settlePreLockAiRun (main.tsx:65,738-770) hand-construct FSM states outside the transition table and store.reset() moves the organ between signals (INV-10.4 violation); offline fallback fabricates source:'brain' signals not in any log (main.tsx:260-272,454)
- Discard rebuilds the marking engine with defaults only — loadedSelectors are not re-seeded (content-loader.content.ts:689-707, main.tsx:1708-1727), deviating from INV-6.6's 'defaults + CSS/AI-selector' clean baseline
- The INV-6.4 two-gate AI machinery (sessionRequiresAiRun vs aiRunUpToDate in lynx/ai-job.ts) and storage/session.ts baseline/draft split are dead code; save gating is the popup matrix approximation; preview mode is a stub (preview.exit.requested/exited emitted by no one)
- Genuinely contract-true and strong: pure domain spine (evaluate.ts single-pass + evaluateBranch, unified rows, verbatim taxonomy, fixed clamp visibility, width-independent Shift climb), popup FSM+frozen memory matrix incl. editor_preparing exemption, typed bus with idempotent reply cache, one nonce-armed page-world program with flush-on-resume timer bridge, token rotation wrapper on all authed calls, backend-issued lock identity persisted per tab, backend-authoritative local-property rule (200 AND 404 clear local; render mode survives 404 only), consent hiding decoupled and high-precision, one-ritual-per-visit with skip-does-not-latch, CDP mobile emulation with UA+client-hints spoofing and posture re-assertion
- markings.changed has a genuine single producer: content reports a monotonic operator-toggle count as a fact, the brain folds and decides the edge (decide.ts:35-46, content-loader.content.ts:31-35) — immune to dynamic-page row-count drift; seeded marks reach the popup as display-only rows without dirtying
- Entanglement creep is concentrated and recent: main.tsx grew from a thin mount to 1,800 lines in the ~15 newest commits (f182855b +251, e0a78987 +169, render-mode series, consent/ritual series); organ files (machine/memory/store/view) stayed clean — the rot is confined to the entrypoints, which are the coordination layer the rewrite existed to fix
- All gates green: pnpm test 58 files/485 tests pass in 8s; the anti-thin-cutover reachability guard (tests/integration/rewrite-cutover.test.ts) was not weakened and passes honestly; the five legacy god-files are deleted and the raw uf.rewriteBrain.* runtime protocol is disabled (no-op addMessageListener, index.ts:70)
- ai.run polls for up to 8 minutes inside one bus request with no keepalive acquisition (index.ts:278-283); run-record heartbeats are persisted but nothing resumes an interrupted run after SW death
- Candidacy was redesigned: 'candidate page' = has a stored pageMarkings record from /load (index.ts:222-267), not the GraphQL propertyPageTypes feed (INV-1.4); propertyPageTypes/cssInfo/updateScrapingConditions builders exist with no popup surface (no Todo list, no send-to-Lynx)

### Open questions raised

- What are the owned backend's /save semantics — full property replace or per-page merge? If full-replace (as remote-api.md A.2/A.5 implies), the rewrite's single-page save snapshot destroys sibling pages' markings and a guard (refuse shrinking saves) or multi-page snapshot assembly must be specified. This is the direct successor to the legacy destructive-save incident and only the backend owner can answer it.
- Is the single-page, never-persisted marking session an accepted product redesign (one page marked+saved at a time), or must the rewrite restore legacy's multi-page property corpus (mark several pages, run AI over the stored corpus, save all at once per INV-5.15/6.5)? The current architecture cannot express the latter without a storage model change.
- Must the editor lock be held while the side panel is closed? The contract implies yes (claim on candidacy, heartbeat while interacted <30min), the implementation ties heartbeats to the open popup's 500ms poll — a product decision on lock semantics is needed before calling this a bug or a simplification.
- Are static-render-mode properties in scope for the alpha? Run AI/Save on them is structurally impossible today (no static-HTML fetcher); either port a fetchStaticPageHtml equivalent or explicitly exclude static properties from the rewrite's first cutover.
- Is the operator-judged render-mode inspection (reload with/without JS, human compares, CTA commits — no automated capture sequence) an accepted replacement for INV-8.6's capture-rendered-then-static sequence, given the rewrite deliberately decoupled device size from JS execution?
- Is candidacy-from-stored-page-records an accepted replacement for GraphQL propertyPageTypes candidacy (INV-1.4), and are the Todo list / candidate badges / send-to-Lynx surfaces required for the rewrite's first production cutover or deferred?
- Does the reflex-arc doctrine still govern acceptance? If yes, the popup-as-orchestrator implementation (signals born at popup, popup-composed content directives, no boot adoption) needs an explicit corrective mandate before more features accrete onto main.tsx; if the doctrine is being consciously relaxed to 'one popup orchestrator over clean organs', the .reimplementation docs should be amended so future audits measure against the real target.

---

## legacy-popup-ux

### Summary

Produced an exhaustive UX/visual spec of the legacy Unfluffify popup from full reads of popup.ts (10,003 lines), popup/ui.tsx (2,783), all popup/* support modules, the three theme CSS files, popup.css, fonts/icon assets, manifest wiring, and the background action/side-panel code. The report documents: the surface itself (it is a per-tab Chrome SIDE PANEL opened by action.onClicked — no default_popup exists); the two-state action icon (green-badged "active" mascot vs plain "default", switched per tab by updateActionForTab, no badge text); the complete design system (Inter/JetBrains Mono, 16-theme light-dark token catalog with Nordic/System as the production default, every component style with px metrics, full MDI icon inventory, mascot logo at 110px); all views (Loading, Configuration/signed-out, Marking with its three postures, preview sidebar, Lynx-checklist modal, property-lock strip) with exact conditions; every control with id/label/gating/click behavior; every notice, status line, toast, and window.confirm/alert string verbatim with file:line citations; the reflex-arc marking-session state machine's frozen per-state button matrix; the complete spinner-contract phase table and curtain priority ladder; login/token UX; the Run-AI, Save/Discard, Send-to-Lynx (cssInfo fail-closed gate) and render-mode wizard flows; popup lifecycle across open/tab-switch/navigate/close; debug affordances (?debugTabId, ?directMode, __UNFLUFFIFY_POPUP_DEBUG__); and the critical finding that ALL feature flags ship false, hiding themes, desktop preview, property-lock, cache tools, trace diagnostics, and preview categories. A vestigial-state section lists view fields (Server Sync panel, Marked Pages list, render-mode warning popover, manual emulation controls) that exist in state but render no UI.

### Key findings

- The legacy 'popup' is actually Chrome's per-tab SIDE PANEL: manifest action has only default_title (no default_popup); action.onClicked binds popup.html via sidePanel.setOptions/open (wxt.config.ts:7-9, background.ts:4285-4294). It persists across tab switches and page navigations, re-binding via tabs.onActivated/onUpdated listeners.
- ALL 10 feature flags are false in production (common/feature-flags.ts:3-17): theme customization (always Nordic/System), desktop preview, device-emulation toggle, property-lock collaboration, cache/unregister tools, trace diagnostics, preview expanded states, page-type change detection, and page-type assignments are all built but shipped dark.
- Marking action buttons render from a finite-state machine's frozen per-state matrix (popup/marking-session-machine.ts:401-512), not live derivation: e.g. pre_ai_clean = only Run AI enabled; post_ai_clean = Show Content List/Save/Discard enabled with the marking toggle LOCKED; running = everything locked behind a 'Computing selectors' curtain with an 8-minute countdown; 30s overlay fail-open.
- One blocking curtain (#ui-curtain) with a strict priority ladder (ui.tsx:657-782) is fed by a shared spinner contract of ~30 named phases with exact titles/notes/timer modes/max durations (common/spinner-contract.ts:171-516); popup holds no local spinner state — it renders brain-broadcast leases.
- Signed-out state IS the Configuration view: token invalidation force-locks the popup there (popup.ts:4158-4171); view resolution requires configEndpoint+aiEndpoint+stageBase+token to reach Marking (popup.ts:5633-5637,5836-5847). Login status is only 'Token saved' (green) / 'Login required' (amber).
- Send to Lynx is a two-stage flow: gate-checked button opens a modal checklist showing per-page-type coverage with up to 3 candidate-URL hints, then a fail-closed backend cssInfo staleness gate ('Checking Lynx selector status...' → clear/match/error) must return 'clear' before the Send button enables (popup.ts:8953-8997,6920-6947).
- Every user-visible string lives centralized in common/text.ts (562 lines) — the report reproduces all of them verbatim including 8 confirm/alert dialogs, ~60 toasts, and the status-line tone tables.
- Visual system: Inter 13px base / JetBrains Mono accents, 16 themes each defined light+dark via CSS light-dark(), cards with 12px radius, 10px body padding, min-width 320px, MDI icon font, mascot logo at 110px; toast = 1800ms bottom bar; modal scrim = 40% ink + 2px blur.
- Debug affordances: ?debugTabId=<id> works in any build to bind the panel to an arbitrary tab; ?directMode=1 (debug builds only) bypasses property/render-mode/page-type gates for marking; window.__UNFLUFFIFY_POPUP_DEBUG__ exposes state for tests; localStorage.ufDebugSpinnerQueue traces curtain decisions.
- Vestigial state that must NOT get UI: Server Sync panel strings (Latest loaded/saved), Marked Pages list, explicit exclude/include lists, the 'How to Verify the Render Mode Manually' popover, and manual device-emulation controls all exist in view state/handlers but have no JSX (verified by grep against ui.tsx).

### Open questions raised

- Should the rewrite keep the side-panel surface (persists across tab switches/navigations) or move to a conventional action popup? This is a product decision the legacy code cannot answer — the entire tab-rebinding lifecycle exists only because of the side panel.
- All 10 feature flags ship false: which of the dark-shipped surfaces (16-theme appearance customization, property-lock collaboration, desktop preview, cache/unregister tools, preview category states) are actually wanted in the rewrite, and which should be dropped rather than ported dark?
- The mobile-simulation-required save gate ('Mobile simulation must be enabled to save markings.') persists even though the manual emulation toggle was removed from the UI — is the intended product rule 'mobile emulation is always on and implicit', making the gate/toast dead copy to drop?
- Send-to-Lynx's cssInfo gate disables Send when Lynx already has matching selectors — is 'cannot re-send identical selectors' the desired product behavior, or should an idempotent re-send be allowed in the rewrite?
- The post-save contract drops the user from marking back to silent mode (session must be re-entered via Enable Marking) — is this intended UX for the rewrite, or an artifact of the legacy save/reconciliation design?

---

## legacy-feature-flows

### Summary

Full feature/flow inventory of legacy Unfluffify v1.10.0 (main @ 28974c2a) written to the report path. It documents, with file:line evidence: (1) property resolution via GraphQL urlSearchInfo with canonical-baseUrl adoption and stale-siteId revalidation, split between a SW page-data lifecycle (sole config/siteId write authority, complete-replace on 200 /load, local wipe on 404) and a popup ensureBaseUrlSiteId path; (2) activation lifecycle — passive loads on configured properties do consent-hiding only, popup bootstrap is the real editor activation, marking never survives any navigation, cross-property navigation resets all volatile tab state; (3) render mode — two manual JS-on/JS-off inspection reloads (debugger-captured when no-JS), backend auto-detection compiled off, "Set" writes local config only (backend persistence rides /save and updateScrapingConditions), selectors older than renderModeUpdatedAt are silently invalidated; (4) marking session driven by a memorized-state FSM: enable reseeds the page entry from defaults+selectors, Save requires a fresh AI run, a successful Save ends the session into silent mode, Discard restores the backend-saved baseline while staying in marking; (5) AI run — stored-evidence-only payload (renderedHtml+submissionXpaths per candidate page, rawHtml backfill, offscreen XPath refinement for static mode), POST /get_selectors with 5s polling / 8min deadline, session-storage record enabling resume, compute lock pausing edits; (6) /save v5 payload assembly with a candidate/backend-saved page filter (the half-snapshot destructive-save exposure), retry/merge/replace semantics; (7) property lock — full WS contract (30s heartbeats, 70s loss/off-candidate windows, 30s cross-property cooldown, takeover/transfer, once-per-minute observer refresh) but the feature flag is FALSE in production, as are desktopPreview, renderModeAutoDetection, cacheAndUnregisterTools, pageTypeAssignments, and others; (8) accounts — login/validate via accounts.{stageBase}, x-update-token rotation on every response, 10-min background alarm validation; (9) Todo list from propertyPageTypes counting backend-saved markings only, Send to Lynx behind a fail-closed cssInfo staleness gate ending in updateScrapingConditions + submittedSelectorsFingerprint; (10) emulation (default mobile 412x960, forced during marking), silent highlighting/consent/motion-freeze contracts, keepalive/alarms/transfer-payload infrastructure, and a storage inventory. Risk notes tie the code paths to the known live findings.

### Key findings

- ALL feature flags are compile-time false in production (src/common/feature-flags.ts:3-17): propertyLockCollaboration, desktopPreview, deviceEmulationToggle, renderModeAutoDetection, cacheAndUnregisterTools, pageTypeAssignments, pageTypesChangeDetection, previewExpandedStates, appearanceCustomization — the entire property-lock system and render-mode auto-detection described in the docs are OFF for real users
- Render-mode 'Set' is local-only: handleRenderModeSet (src/popup.ts:6995-6999) writes renderMode+renderModeUpdatedAt to IndexedDB config only; backend persistence happens via the /save payload and the renderingMode enum on updateScrapingConditions; selectors whose selectorsUpdatedAt predates renderModeUpdatedAt are silently treated as absent (config.ts:745-767)
- A 200 /load COMPLETELY replaces the local property config (replaceServerConfigIntoLocalSnapshot, remote-config-sync.ts:316-335) and a 404 wipes local markings+selectors (clearLocalPageDataForMissingRemote); /load fires once per page session (#load-once) and the lock-editor path skips it after a one-time bootstrap
- Save Session ends the marking session: handlePageSave syncs with includeAllLocalPageMarkings+replaceLocalFromServerResponse then applyPostSaveSilentTransition drops the tab to silent mode (page-reconciliation.ts:160-179, popup.ts:8187-8222); Discard restores the backend-saved baseline via configUpdated{forceReloadPageEntry:true} while staying in marking (popup.ts:8224-8302)
- Save is gated on AI freshness: doesSessionRequireAiRun (popup.ts:2611-2640) blocks save until the AI run fingerprint matches current markings; the FSM (marking-session-machine.ts) encodes pre_ai_clean/pre_ai_dirty/running/post_ai_clean button matrices as frozen memory
- AI run: payload = stored evidence only (pages with renderedHtml+submissionXpaths within baseUrl; dirty current page is snapshot-captured first with a 120s budget; rawHtml backfilled by background fetch; static mode adds rawHtml + offscreen DOMParser XPath refinement), POST /get_selectors -> {session_id}, poll status every 5s to an 8-minute deadline, resumable via a session-storage record with 2-min TTL heartbeats and a fail-open compute lock
- Destructive-save exposure confirmed in code: non-page-save sync paths filter payload pageMarkings to backendSavedPageUrls (remote-config.ts:420-423), so an empty backend-saved cache produces a 200 /save with empty pageMarkings — the known production wipe; the dropped guard was dangling commit e11059b1
- Marking never survives navigation ('editor-mobile-only contract', background.ts:3897-3908) and cross-property navigation resets initial.active + brain editor gate so the new property loads passively (consent-hiding only, background.ts:3876-3893, 4228-4240)
- Property lock contract (PROPERTY_LOCK.md + property-lock-background.ts): sessionStorage clientId identity, 30s heartbeats while active <30min, 70s connection-loss and off-candidate windows, 30s cross-property recovery, same-user continue/takeover flows, immediate release on tab close, observer /load refresh max once/minute — all implemented but flag-disabled
- Send to Lynx: Save button opens a checklist popover requiring every page type covered by BACKEND-SAVED markings, then a fail-closed cssInfo staleness gate (sanitized set equality, lynx-checklist.ts:450-484) before updateScrapingConditions(domainId, includeCss, excludeCss, renderingMode) and a submittedSelectorsFingerprint write + follow-up config sync
- Token rotation is ambient: every backend/GraphQL/accounts response can carry x-update-token which is persisted to storage.sync (lynx-live-pages.ts:141-161); a 10-min background alarm validates the token and pushes tokenInvalid to lock the popup config; changing endpoint origin or stage base clears the token
- Device emulation defaults to mobile 412x960 on every popup bootstrap/marking activation, is forced back on during marking via debugger.onDetach, and is a precondition for AI snapshot capture; the manual toggle and desktop preview are flag-disabled

### Open questions raised

- Should the rewrite ship property-lock collaboration enabled, keep it compiled off like legacy production, or drop it? Legacy carries the full locked contract (PROPERTY_LOCK.md) but no production user has ever run it
- Is the legacy /save contract (server treats POST /save as full-replace of pageMarkings) the intended backend semantics the rewrite must defend against, or will the backend gain a merge/guard so an empty-pageMarkings save cannot wipe a property?
- Which flag-disabled capabilities are product-desired for the rewrite: desktop preview, render-mode auto-detection (/is_js_rendered), page-type assignments (/assign_page_types), cache/unregister tools, manual device-emulation toggle?
- Is 'Save Session ends the session and drops to silent mode' a UX behavior to preserve, or an accident of the legacy post-save contract the rewrite may change (e.g. stay in marking after save)?
- Should Todo/checklist completion continue to count only backend-saved page markings (never local drafts), and should Send to Lynx remain restricted to the silent-highlighting surface?
- Render-mode changes silently invalidate previously computed AI selectors (timestamp rule) — is that invalidation a product requirement to keep, and should the user be told when it happens?

---

## legacy-arch-weaknesses

### Summary

Cataloged 49 testable weaknesses (W-01..W-49) of legacy Unfluffify v1.10.0, each with file:line evidence, the bug it caused, and the correct-design counterpart, plus a 25-row rewrite acceptance checklist. The system runs nine parallel transports (88 raw message-type strings beside a typed bus, ports, a page-world relay, a TTL'd payload store, CDP, and an offscreen doc) dispatched through a 47-branch if-chain in background.ts. Decisions are multi-owner everywhere: marking-enabled derives from five competing sources in a ~1,914-line non-serialized refreshUiInner whose interleaved passes required four hand-maintained guard systems (epochs, seq stamps, raise-only latches, source-omission heuristics); AI-run authority is negotiated by a five-field pattern match; popup presentation is a three-layer override sandwich. State is flat mutable singletons (~180 popup fields, 112 content fields), a whole-map read-modify-write config store written by three realms with per-context-only queues, five coexisting per-tab background stores with divergent lifetimes (the direct cause of the MV3 stuck-spinner bug), and sticky level-snapshots re-folded by a 1 Hz heartbeat where one forgotten republish wedges the system permanently. Save integrity is the gravest area: /save is a full-snapshot replace behind a coverage filter that emitted pageMarkings:{} and wiped a production property on a 200 response — the guard fix exists only as dangling commit e11059b1, not on main; AI-computed selectors are persisted only by the popup on the happy path and the resume path deletes the run record on any hiccup, matching the observed intermittent selector loss. Lifecycle bugs include the reveal ritual latch consumed on attempt rather than success, brain.disposeTab documented known-unsafe for navigation, popup-dependent content activation, and a render-mode inspection saga with no durable record that leaves deleted-record properties un-bootstrappable. Error handling swallows causes into {ok:false} (73 bare catches in background.ts alone) behind generic toasts like "Unable to save session", which is why the data wipe looked like a successful save. 77 of 202 test files pin source text with regexes instead of behavior, and core flows are verifiable only through documented-flaky live CDP harnesses.

### Key findings

- Production data loss is structural, not incidental: /save full-snapshot replace + coverage filter keyed on a best-effort backend-saved cache emitted pageMarkings:{} and wiped a property on HTTP 200 (remote-config.ts:420-445); the guard exists only as dangling commit e11059b1 — production main today has no protection.
- AI selector persistence has a single fragile writer: only popup applyComputedSelectorSet (popup.ts:8396) writes config.selectors, after the run roundtrip resolves in the popup; popup closure loses the result and the resume path (popup.ts:6370-6470) deletes the persisted run record on any status hiccup — the mechanism behind the observed intermittent selector loss.
- Marking-enabled has five competing sources reconciled inside the ~1,914-line non-serialized refreshUiInner (popup.ts:5106-5226), including a content probe whose conclusion the popup writes back into background tab state; the documented #5/#14 session-collapse family and the epoch/seq/latch guard doctrine all exist to defend against this one function's interleaving.
- Coordination is level-triggered and sticky: popup/content accumulate merged fact snapshots re-pulled by a 1 Hz brain heartbeat (heartbeat.ts:46-51), so any teardown that forgets to republish a fact leaves it re-folded forever (knowledge.md:603); the store also schedules a projection on every mutate including no-op folds, and the resulting publish->fold->project->apply loop (~200/sec) was patched with a dedup cache rather than removed.
- One-shot lifecycle latches are consumed on ATTEMPT, not success: consumePageVisitRevealFreezeAttempt (content-main.ts:2180-2189) permanently blocks the reveal/freeze ritual for a page visit after any early failure — the exact bug the rewrite's 'let the ritual survive an early attempt' commit fixes.
- brain.disposeTab is documented known-unsafe for navigation (deletes popupPortCounts, corrupting fact-source arbitration while the popup stays connected — brain/index.ts:1007-1017, knowledge.md:552); navigation teardown is instead spread across four uncoordinated handlers with different definitions of 'navigated'.
- The config store is whole-map read-modify-write from three realms (popup, content, background all call updateConfig/saveConfigs) serialized only per-JS-context (config.ts:35-36), and getConfigs writes back during reads — cross-realm lost updates are structurally possible.
- MV3 restart-safety is achieved by hacks: a refcounted 20s keepalive, a 30s spinner expiry grace projecting away stuck curtains caused by the queue-in-memory/selection-persisted split (spinner-authority.ts:22-49), and session-storage brain state that resurrects stale preview-open wedges across SW reloads (knowledge.md:608).
- Legacy mobile emulation spoofs device metrics only via chrome.debugger (emulation.ts:330-376) — no user-agent/client-hints identity — so UA-sniffing sites serve desktop DOM into the mobile viewport and saved snapshots inherit the hybrid.
- Error handling masks causes end-to-end: 73 bare .catch(() => {ok:false}) handlers in background.ts, replyWithKeepAlive maps any failure to a fallback with no log, and the user-facing surface is generic text ('Unable to save session', text.ts:438) — why the production wipe presented as a successful save.
- Testability is inverted: 77 of 202 test files readFileSync production source and assert regexes over it (source-contract pins), while real behavioral verification requires documented-flaky live CDP harnesses with trusted-input, dialog-safety, and SW-cache pitfalls filling the knowledge base.
- Four god files hold 57% of the source (core.ts 14,312 / popup.ts 10,003 / content-main.ts 7,557 / background.ts 4,301 lines) and core.ts is formally edit-frozen without an approved plan; business predicates are duplicated across bundles and kept equal by comments and byte-identity tests (the eval'd page-motion freeze pair).

### Open questions raised

- Should /save remain a full-snapshot-replace API (requiring client-side destructive-write guards), or should the backend move to keyed per-page/delta writes? The wipe class is only fully closable server-side.
- What is the intended recovery UX for a property whose backend config record was deleted mid-lifecycle: silent auto-recreation from local state, or an explicit re-onboarding flow (render-mode re-inspection required)?
- Should the content engine (consent hiding + reveal/freeze + silent highlights) run on EVERY configured property page at load for every editor, accepting the page-load performance cost, or only on popup-engaged tabs? Legacy broadened this in Round 3 with an explicitly flagged unvalidated perf risk.
- When an AI run completes after the popup has closed, should the background persist and surface the result on next popup open (and should the user be charged/notified for runs whose results were auto-persisted without review)?
- Is the 'marking never survives navigation/reload' contract desired product behavior, or should an accidental same-URL reload resume the marking session with its unsaved draft?
- How deep should mobile identity spoofing go (UA string only, full UA-CH, touch/pointer media, platform hints)? The target definition — e.g. Googlebot-smartphone parity — determines both implementation and what the saved snapshots represent.
- Should destructive confirmations (discard, transfer, cache clear) remain native blocking dialogs or move to non-blocking in-popup UI? Legacy's window.confirm calls freeze the fact pipeline and break automation, but native dialogs are the established editor UX.

---

## All open questions pooled (Q&A candidates for the architect)

- **[legacy-locked-contracts]** Does the rewrite adopt the legacy user-specified page-block scope verbatim (block the page only when popup is busy AND interaction can affect results: reveal/freeze, AI run, save) or the stricter earlier rule (every popup curtain blocks the page)? Legacy docs record both, with the relaxed version as the last user word (lifecycle-resume-plan.md:388-390).
- **[legacy-locked-contracts]** The curtain/spinner ownership migration to per-layer orchestrators (architect direction, knowledge.md:549) was never completed in legacy — the brain is still authoritative for sessionCurtainPhase. Should the rewrite treat the layer-orchestrator model as the binding target architecture, or is the shipped brain-authoritative P4-P6 state the contract?
- **[legacy-locked-contracts]** Task #18 (audit all fail-open API calls and decide which flip to fail-closed) was explicitly queued for the architect's decision table and never resolved — the rewrite needs that decision table to know which network failures must block vs degrade.
- **[legacy-locked-contracts]** Legacy forces desktop-preview availability only when the property already has AI selectors and disables marking entry while it is on; is that gating a product requirement to preserve, or an implementation artifact of the CDP emulation debugger that the rewrite may relax?
- **[legacy-locked-contracts]** The accepted F2 tradeoff (landmark-less full-width columns are widen-eligible again) was called an 'accepted tradeoff' by the architect for the bonliva footer case — does the product owner accept the same over-widening risk in the rewrite, or want a different restraint now that the code is being rebuilt?
- **[legacy-locked-contracts]** The pageTypeAssignments feature flag stayed false pending a backend endpoint (HANDOFF.md:185) — is that endpoint now live, and is the feature in scope for the rewrite?
- **[legacy-locked-contracts]** Legacy's off-candidate same-property pages keep silent highlighting and lock visibility while blocking only marking entry; confirm the rewrite must reproduce this exact partial-degradation UX rather than collapsing the page UI.
- **[legacy-content-ux]** Should the rewrite keep the marking overlay click-capture model (right-click/contextmenu also commits a mark, core.ts:9715-9717), or reserve right-click for the browser context menu?
- **[legacy-content-ux]** The legacy freeze is page-visit-sticky: after disabling marking the page stays frozen (animations/media halted) until navigation. Is that the desired product behavior for the rewrite, or should disabling fully release the page?
- **[legacy-content-ux]** Legacy hides consent chrome on every configured property page even for passive visitors who are not editing - is silently suppressing cookie/consent banners for all team members on production customer sites the intended scope, given GDPR-consent implications of the operator never being able to interact with the banner?
- **[legacy-content-ux]** Does the rewrite need to preserve the click-to-copy tooltip affordance in silent mode (left-click on highlighted content copies selector+XPath), which hijacks normal page clicks on annotated nodes?
- **[legacy-content-ux]** Should the rewrite adopt the legacy one-per-visit reveal ritual timings (up to 10 scroll passes x 1s dwell, potentially ~20-30s of blocked input on long pages) or is a faster/interruptible reveal acceptable?
- **[legacy-content-ux]** Is the debugger-based viewport emulation (with Chrome's 'started debugging' infobar) acceptable long-term, and should the rewrite's UA/client-hints spoofing (Pixel 7/Android 13) be backported as the canonical behavior for parity of what editors mark vs what the AI judges?
- **[rewrite-architecture]** What are the owned backend's /save semantics — full property replace or per-page merge? If full-replace (as remote-api.md A.2/A.5 implies), the rewrite's single-page save snapshot destroys sibling pages' markings and a guard (refuse shrinking saves) or multi-page snapshot assembly must be specified. This is the direct successor to the legacy destructive-save incident and only the backend owner can answer it.
- **[rewrite-architecture]** Is the single-page, never-persisted marking session an accepted product redesign (one page marked+saved at a time), or must the rewrite restore legacy's multi-page property corpus (mark several pages, run AI over the stored corpus, save all at once per INV-5.15/6.5)? The current architecture cannot express the latter without a storage model change.
- **[rewrite-architecture]** Must the editor lock be held while the side panel is closed? The contract implies yes (claim on candidacy, heartbeat while interacted <30min), the implementation ties heartbeats to the open popup's 500ms poll — a product decision on lock semantics is needed before calling this a bug or a simplification.
- **[rewrite-architecture]** Are static-render-mode properties in scope for the alpha? Run AI/Save on them is structurally impossible today (no static-HTML fetcher); either port a fetchStaticPageHtml equivalent or explicitly exclude static properties from the rewrite's first cutover.
- **[rewrite-architecture]** Is the operator-judged render-mode inspection (reload with/without JS, human compares, CTA commits — no automated capture sequence) an accepted replacement for INV-8.6's capture-rendered-then-static sequence, given the rewrite deliberately decoupled device size from JS execution?
- **[rewrite-architecture]** Is candidacy-from-stored-page-records an accepted replacement for GraphQL propertyPageTypes candidacy (INV-1.4), and are the Todo list / candidate badges / send-to-Lynx surfaces required for the rewrite's first production cutover or deferred?
- **[rewrite-architecture]** Does the reflex-arc doctrine still govern acceptance? If yes, the popup-as-orchestrator implementation (signals born at popup, popup-composed content directives, no boot adoption) needs an explicit corrective mandate before more features accrete onto main.tsx; if the doctrine is being consciously relaxed to 'one popup orchestrator over clean organs', the .reimplementation docs should be amended so future audits measure against the real target.
- **[legacy-popup-ux]** Should the rewrite keep the side-panel surface (persists across tab switches/navigations) or move to a conventional action popup? This is a product decision the legacy code cannot answer — the entire tab-rebinding lifecycle exists only because of the side panel.
- **[legacy-popup-ux]** All 10 feature flags ship false: which of the dark-shipped surfaces (16-theme appearance customization, property-lock collaboration, desktop preview, cache/unregister tools, preview category states) are actually wanted in the rewrite, and which should be dropped rather than ported dark?
- **[legacy-popup-ux]** The mobile-simulation-required save gate ('Mobile simulation must be enabled to save markings.') persists even though the manual emulation toggle was removed from the UI — is the intended product rule 'mobile emulation is always on and implicit', making the gate/toast dead copy to drop?
- **[legacy-popup-ux]** Send-to-Lynx's cssInfo gate disables Send when Lynx already has matching selectors — is 'cannot re-send identical selectors' the desired product behavior, or should an idempotent re-send be allowed in the rewrite?
- **[legacy-popup-ux]** The post-save contract drops the user from marking back to silent mode (session must be re-entered via Enable Marking) — is this intended UX for the rewrite, or an artifact of the legacy save/reconciliation design?
- **[legacy-feature-flows]** Should the rewrite ship property-lock collaboration enabled, keep it compiled off like legacy production, or drop it? Legacy carries the full locked contract (PROPERTY_LOCK.md) but no production user has ever run it
- **[legacy-feature-flows]** Is the legacy /save contract (server treats POST /save as full-replace of pageMarkings) the intended backend semantics the rewrite must defend against, or will the backend gain a merge/guard so an empty-pageMarkings save cannot wipe a property?
- **[legacy-feature-flows]** Which flag-disabled capabilities are product-desired for the rewrite: desktop preview, render-mode auto-detection (/is_js_rendered), page-type assignments (/assign_page_types), cache/unregister tools, manual device-emulation toggle?
- **[legacy-feature-flows]** Is 'Save Session ends the session and drops to silent mode' a UX behavior to preserve, or an accident of the legacy post-save contract the rewrite may change (e.g. stay in marking after save)?
- **[legacy-feature-flows]** Should Todo/checklist completion continue to count only backend-saved page markings (never local drafts), and should Send to Lynx remain restricted to the silent-highlighting surface?
- **[legacy-feature-flows]** Render-mode changes silently invalidate previously computed AI selectors (timestamp rule) — is that invalidation a product requirement to keep, and should the user be told when it happens?
- **[legacy-arch-weaknesses]** Should /save remain a full-snapshot-replace API (requiring client-side destructive-write guards), or should the backend move to keyed per-page/delta writes? The wipe class is only fully closable server-side.
- **[legacy-arch-weaknesses]** What is the intended recovery UX for a property whose backend config record was deleted mid-lifecycle: silent auto-recreation from local state, or an explicit re-onboarding flow (render-mode re-inspection required)?
- **[legacy-arch-weaknesses]** Should the content engine (consent hiding + reveal/freeze + silent highlights) run on EVERY configured property page at load for every editor, accepting the page-load performance cost, or only on popup-engaged tabs? Legacy broadened this in Round 3 with an explicitly flagged unvalidated perf risk.
- **[legacy-arch-weaknesses]** When an AI run completes after the popup has closed, should the background persist and surface the result on next popup open (and should the user be charged/notified for runs whose results were auto-persisted without review)?
- **[legacy-arch-weaknesses]** Is the 'marking never survives navigation/reload' contract desired product behavior, or should an accidental same-URL reload resume the marking session with its unsaved draft?
- **[legacy-arch-weaknesses]** How deep should mobile identity spoofing go (UA string only, full UA-CH, touch/pointer media, platform hints)? The target definition — e.g. Googlebot-smartphone parity — determines both implementation and what the saved snapshots represent.
- **[legacy-arch-weaknesses]** Should destructive confirmations (discard, transfer, cache clear) remain native blocking dialogs or move to non-blocking in-popup UI? Legacy's window.confirm calls freeze the fact pipeline and break automation, but native dialogs are the established editor UX.
